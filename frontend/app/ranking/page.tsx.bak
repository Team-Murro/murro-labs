// frontend/app/ranking/page.tsx
'use client';

import { useState, useEffect } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';

interface TopStore {
  store_name: string;
  address: string;
  lat: number;
  lng: number;
  "1st": number;
  "2nd": number;
}

export default function RankingPage() {
  const [stores, setStores] = useState<TopStore[]>([]);
  const [loading, setLoading] = useState(true);
  const router = useRouter();
  const API_BASE = '';

  useEffect(() => {
    fetch(`${API_BASE}/api/stores/top`)
      .then(res => res.json())
      .then(data => {
        setStores(data);
        setLoading(false);
      })
      .catch(err => {
        console.error(err);
        setLoading(false);
      });
  }, []);

  // 클릭 시 지도 페이지로 이동하며 위치 전달
  const handleRowClick = (store: TopStore) => {
    router.push(`/map?lat=${store.lat}&lng=${store.lng}&name=${encodeURIComponent(store.store_name)}`);
  };

  return (
    <div className="min-h-screen bg-gray-900 text-white p-4 font-sans">
      {/* 헤더 */}
      <div className="flex items-center justify-between mb-6 max-w-4xl mx-auto mt-4">
        <Link href="/" className="bg-gray-800 px-4 py-2 rounded-xl text-gray-300 hover:text-white hover:bg-gray-700 transition-all font-bold text-sm">
          ⬅ 뒤로가기
        </Link>
        <h1 className="text-2xl md:text-3xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-500">
          🏆 역대 로또 명당 TOP 100
        </h1>
        <div className="w-20"></div> {/* 레이아웃 균형용 빈칸 */}
      </div>

      <div className="max-w-4xl mx-auto bg-gray-800 rounded-3xl border border-gray-700 shadow-2xl overflow-hidden">
        {loading ? (
          <div className="p-20 text-center text-gray-400 animate-pulse">
            명당 데이터를 집계 중입니다... 📊
          </div>
        ) : (
          <div className="overflow-x-auto">
            <table className="w-full text-left border-collapse">
              <thead className="text-gray-400 uppercase bg-gray-900/50 sticky top-0 z-10 text-xs md:text-sm">
                <tr>
                  <th className="px-4 py-4 text-center font-bold">순위</th>
                  <th className="px-4 py-4 font-bold">판매점 정보 (누르면 지도로 이동)</th>
                  <th className="px-4 py-4 text-center text-red-400 font-extrabold whitespace-nowrap">1등 횟수</th>
                  <th className="px-4 py-4 text-center text-blue-400 font-bold whitespace-nowrap">2등 횟수</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-gray-700 text-sm md:text-base">
                {stores.map((store, index) => (
                  <tr 
                    key={index} 
                    onClick={() => handleRowClick(store)}
                    className="hover:bg-gray-700/50 cursor-pointer transition-colors group"
                  >
                    <td className="px-4 py-4 text-center font-bold text-xl text-gray-500 group-hover:text-white font-mono">
                      {index + 1}
                    </td>
                    <td className="px-4 py-4">
                      <div className="font-bold text-lg text-gray-200 group-hover:text-yellow-300 transition-colors mb-1">
                        {store.store_name}
                      </div>
                      <div className="text-gray-500 text-xs md:text-sm flex items-center gap-1">
                        📍 {store.address}
                      </div>
                    </td>
                    <td className="px-4 py-4 text-center">
                      <span className="inline-block bg-red-900/30 text-red-400 border border-red-900/50 px-3 py-1 rounded-lg font-bold">
                        {store["1st"]}회
                      </span>
                    </td>
                    <td className="px-4 py-4 text-center text-gray-400">
                      {store["2nd"]}회
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}
      </div>
    </div>
  );
}
